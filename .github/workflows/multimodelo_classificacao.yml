name: Multimodelo - classificacao completa

# Materializa CLASSIF__<modelo> para cada IA selecionada, com predicao
# out-of-fold e lote dinamico.
#
# AUTOMATICO A CADA 15 MIN (MESMO CRON DA ETAPA 1/LSTM), OS 7 MODELOS
# OPERACIONAIS NO CRON (6 leves + lstm): classificam os chamados novos no
# mesmo ritmo do LSTM oficial. max_turnos=0 = todos os pendentes. E incremental
# e barato depois da materializacao inicial: classificacao_multimodelo.py so
# processa as linhas ainda nao feitas e encerra rapido quando nao ha pendentes
# (no-op) — so fica pesado de novo apos um reset ou grande avanco da base.
#
# transformer_ft FICA FORA da selecao automatica do schedule (auditoria
# A4-2, 2026-08-20): este cron nunca instala torch/transformers (so
# tensorflow, para o lstm), entao o transformer_ft sempre cai em fallback
# LSTM/RF e o proprio classificacao_multimodelo.py RECUSA publicar esse
# fallback sob o nome do modelo pedido — nos runs auditados, ~10 dos ~11 min
# do step "Classificar por modelo" eram gastos treinando esse fallback so
# para descarta-lo. Nao e remocao cientifica do modelo: transformer_ft
# continua disponivel via workflow_dispatch (input 'modelos': pesados/todos/
# explicito) e no seu workflow dedicado (.github/workflows/transformer_ft.yml),
# que instala as dependencias reais.
# Compartilha o concurrency 'multimodelo-pipeline' com a reclassificacao
# multimodelo (multimodelo_reclassificacao.yml): a reclassificacao LE
# CLASSIF__<modelo>, que esta classificacao ESCREVE, entao as duas nunca
# rodam ao mesmo tempo (mas nenhuma cancela a outra em andamento).

on:
  # Cron reativado em 02/08/2026, depois de corrigidas as DUAS causas do
  # incidente daquele dia:
  #
  # 1. DUPLICACAO. `ids_ja_classificados` lia a coluna B (linha_planilha).
  #    Quando a base caiu de 14.094 para 14.058, todo chamado mudou de linha,
  #    apareceu como pendente e foi reclassificado e ACRESCENTADO:
  #    CLASSIF__extra_trees terminou com 28.152 registros. Agora le a coluna C
  #    (id_chamado), entao mudanca de tamanho da base nao gera reclassificacao.
  #
  # 2. VAZAMENTO. A memoria validada entrava no treino com peso 3, e o acerto
  #    validado do extra_trees saltou de 0,7958 para 0,9816 porque o modelo via
  #    a conferencia humana antes de prever. O cron agora roda SEMPRE com
  #    --sem-memoria-validada: estas predicoes existem para serem avaliadas
  #    contra a conferencia humana, e nao podem te-la no treino.
  #
  # O classificador de PRODUCAO (Etapa 1) segue usando a memoria validada, que
  # e onde ela faz sentido: nao repetir erro ja corrigido.
  schedule:
    - cron: "5,20,35,50 * * * *"
  workflow_dispatch:
    inputs:
      modelos:
        description: "leves, pesados, todos ou lista separada por virgula"
        default: "leves"
      max_turnos:
        description: "Turnos de 15 por modelo (0 = todos os pendentes)"
        default: "0"
      sem_memoria_validada:
        description: "Treinar SEM as decisoes humanas (obrigatorio para avaliacao)"
        type: boolean
        default: false
      aplicar:
        description: "Gravar na planilha"
        type: boolean
        default: false
      lstm_perfil:
        description: "Perfil LSTM"
        type: choice
        default: padrao
        options:
          - padrao
          - robusto

concurrency:
  group: multimodelo-pipeline
  cancel-in-progress: false
  queue: max

# Menor escopo: so o job "multimodelo" persiste o marcador de automacao (via
# helper). "guard" so le docs/dados/multimodelo_metricas.json e herda o
# contents:read padrao.
permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    outputs:
      executar: ${{ steps.g.outputs.executar }}
      valor_atual: ${{ steps.g.outputs.valor_atual }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Avaliar avanco da base (>=1000 chamados ou multimodelo vazio)
        id: g
        run: python src/guard_automacao.py --chave multimodelo_classificacao --metrica registros --limiar 1000 --forcar-se-aba-zero multimodelo_metricas

  multimodelo:
    needs: guard
    # Roda no cron diario (acompanha a Etapa 1), no disparo manual, ou quando a guarda
    # de avanco aprovar. O custo e barato quando nao ha pendentes (no-op no script).
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'schedule' || needs.guard.outputs.executar == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write
    env:
      SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      LSTM_PERFIL: ${{ github.event.inputs.lstm_perfil || 'padrao' }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            requirements-leves.txt
            requirements.txt
      - name: Dependencias leves
        run: python -m pip install --retries 5 --timeout 120 -r requirements-leves.txt
      - name: TensorFlow se LSTM estiver no escopo
        if: ${{ github.event_name == 'schedule' || contains(github.event.inputs.modelos, 'lstm') || github.event.inputs.modelos == 'pesados' || github.event.inputs.modelos == 'todos' }}
        run: python -m pip install --retries 5 --timeout 120 tensorflow==2.17.0
      - name: Recriar chave da conta de servico
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          if [ -z "$GCP_SA_KEY" ]; then echo "Secret GCP_SA_KEY ausente." >&2; exit 1; fi
          printf '%s' "$GCP_SA_KEY" > credenciais_sa.json
      - name: Classificar por modelo
        env:
          MODELOS: ${{ github.event.inputs.modelos || 'naive_bayes,regressao_logistica,linear_svc,sgd,extra_trees,random_forest,lstm' }}
          MAX_TURNOS: ${{ github.event.inputs.max_turnos || '0' }}
        run: |
          # No cron (schedule) materializa os 7 modelos operacionais do cron
          # (6 leves + lstm) -- transformer_ft fica de fora (ver comentario no
          # topo do arquivo); manual respeita o input 'modelos'/'aplicar',
          # incluindo 'pesados'/'todos'/transformer_ft explicito.
          APLICAR=""
          if [ "${{ github.event.inputs.aplicar }}" = "true" ] || [ "${{ github.event_name }}" = "schedule" ]; then
            APLICAR="--aplicar"
          fi
          # Sem memoria validada, as predicoes ficam limpas para a avaliacao
          # contra a conferencia humana. Ver o incidente de 02/08/2026.
          # No cron, SEMPRE sem memoria validada: estas predicoes alimentam a
          # avaliacao contra a conferencia humana. No disparo manual, respeita
          # o input (para quem quiser reproduzir o comportamento de producao).
          SEM_MEM=""
          if [ "${{ github.event.inputs.sem_memoria_validada }}" = "true" ] || [ "${{ github.event_name }}" = "schedule" ]; then
            SEM_MEM="--sem-memoria-validada"
          fi
          python src/classificacao_multimodelo.py --modelos "$MODELOS" --max-turnos "$MAX_TURNOS" $APLICAR $SEM_MEM
      - name: Renovar artefatos sanitizados do dashboard
        if: ${{ github.event_name == 'schedule' || github.event.inputs.aplicar == 'true' }}
        run: python src/exportar_classificacao_dashboard.py
      - name: Publicar artefatos sanitizados do dashboard
        if: ${{ github.event_name == 'schedule' || github.event.inputs.aplicar == 'true' }}
        run: python src/persistir_classificacao_dashboard.py
      - name: Remover credenciais
        if: always()
        run: rm -f credenciais_sa.json
      - name: Persistir marcador de automacao
        run: |
          python src/persistir_estado_automacao.py \
            --chave multimodelo_classificacao \
            --valor '${{ needs.guard.outputs.valor_atual || '0' }}' \
            --aplicar
