name: Reclassificacao dry-run

# Testa a Etapa 2 sem gravar na planilha. Usa conta de servico apenas para ler
# os dados e montar o lote. Nao passa --aplicar.

on:
  workflow_dispatch:
    inputs:
      modelo:
        description: "Modelo de reclassificacao"
        type: choice
        default: producao
        options:
          - producao
          - baseline
          - robusto
      max_turnos:
        description: "Turnos de 15 para simular"
        default: "2"
      lstm_perfil:
        description: "Perfil LSTM (padrao ou robusto)"
        type: choice
        default: padrao
        options:
          - padrao
          - robusto

permissions:
  contents: read

jobs:
  dry-run:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      LSTM_PERFIL: ${{ github.event.inputs.lstm_perfil || 'padrao' }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-robusto.txt
      - name: Instalar dependencias
        run: |
          python -m pip install --retries 5 --timeout 120 -r requirements.txt
          if [ "${{ github.event.inputs.modelo || 'producao' }}" = "robusto" ]; then
            python -m pip install --retries 5 --timeout 120 -r requirements-robusto.txt
          fi
      - name: Recriar chave da conta de servico
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          if [ -z "$GCP_SA_KEY" ]; then echo "Secret GCP_SA_KEY ausente." >&2; exit 1; fi
          printf '%s' "$GCP_SA_KEY" > credenciais_sa.json
      - name: Simular reclassificacao sem gravar
        run: >
          python src/executar_etapa2.py
          --modelo "${{ github.event.inputs.modelo || 'producao' }}"
          --max-turnos "${{ github.event.inputs.max_turnos || '2' }}"
      - name: Remover credenciais
        if: always()
        run: rm -f credenciais_sa.json
